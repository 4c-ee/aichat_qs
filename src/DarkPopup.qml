import QtQuick
import QtQuick.Controls

Popup {
    id: root

    modal: true
    focus: true
    padding: 20

    background: Rectangle {
        color: "#1A1A1A"
        border.color: "#404040"
    }
}
